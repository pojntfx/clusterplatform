{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to node process (first: forward port 9229 and select the `package.json` of the service in the editor)",
      "address": "localhost",
      "port": 9229,
      "localRoot": "${fileDirname}",
      "remoteRoot": "/opt/clusterplatform/app"
    }
  ]
}
